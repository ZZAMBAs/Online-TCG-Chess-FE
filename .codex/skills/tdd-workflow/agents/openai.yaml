interface:
  display_name: "TDD Workflow"
  short_description: "이슈 브랜치에서 TDD 전체 흐름 실행"
  default_prompt: "Use $tdd-workflow auth-1 to prepare feature/auth-001, then run RED, GREEN, BLUE, security review, and AC verification."
