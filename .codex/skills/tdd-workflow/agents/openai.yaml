interface:
  display_name: "TDD Workflow"
  short_description: "이슈 브랜치에서 TDD와 단위 커밋 실행"
  default_prompt: "Use $tdd-workflow auth-1 to prepare feature/auth-001, run RED, GREEN, BLUE, security review, optional $git-commit work-unit commits, and AC verification."
