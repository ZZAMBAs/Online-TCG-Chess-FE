interface:
  display_name: "Create TRD"
  short_description: "FE TRD 생성과 build 설정 보정"
  default_prompt: "Use $create-trd to generate FE hub and feature TRDs from approved PRD, design baseline, fixed architecture, storyboard, and contracts."
