interface:
  display_name: "Create PR"
  short_description: "커밋 정리 후 이슈 연결 PR 생성"
  default_prompt: "Use $create-pr to inspect current branch changes, optionally use $git-commit for coherent work-unit commits, run full tests, draft a Korean PR with a same-repo closing keyword, and publish it after approval."
