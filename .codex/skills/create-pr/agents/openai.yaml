interface:
  display_name: "Create PR"
  short_description: "검증 후 이슈 연결 PR 생성 절차"
  default_prompt: "Use $create-pr to inspect current branch changes, find the related current-repo GitHub Issue, run full tests, draft a Korean PR with a same-repo closing keyword, and publish it after approval."
