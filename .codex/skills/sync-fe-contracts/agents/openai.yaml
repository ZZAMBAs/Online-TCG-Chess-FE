interface:
  display_name: "Sync FE Contracts"
  short_description: "BE fixed 계약을 FE 참조 문서로 투영"
  default_prompt: "Use $sync-fe-contracts to project fixed BE negotiation summaries into FE docs/contracts without creating implementation, issue, or TDD plans."
