interface:
  display_name: "Architecture Decision"
  short_description: "승인 디자인 기준 기반 FE 아키텍처 확정"
  default_prompt: "Use $architecture-decision after $design-decision to fix FE architecture, review it, and apply approved build/harness settings."
